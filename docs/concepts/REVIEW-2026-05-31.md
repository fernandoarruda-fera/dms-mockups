# Revisão Cruzada de Concepts — 2026-05-31

Revisão de todos os concepts em `docs/concepts/`. Foco em inconsistências, cross-references quebrados, gaps de definição, conceitos órfãos e funcionalidades implícitas nos mockups sem concept.

**Concepts revisados (12):**
CONCEPT-ACTIONIA, CONCEPT-AI-GENERATION, CONCEPT-ALERT, CONCEPT-MATCH-ENGINE, CONCEPT-MODULE-INTERVENIENTES-CONFIG, CONCEPT-PERMISSIONS, CONCEPT-SAVED-FILTERS, CONCEPT-STAGE-OWNERSHIP-VALIDATION, CONCEPT-STAGE-ROLE-ASSIGNMENT, CONCEPT-USER-PROFILE-PREFERENCES, CONCEPT-USER-SCOPE-INTERVENIENTES, CONCEPT-WORKFLOW-OBLIGATORY.

---

## Achados

| # | Achado | Tipo | Onde | Sugestão |
|---|---|---|---|---|
| 1 | `[[CONCEPT-ALCADAS]]` referenciado mas não existe como doc | Cross-reference quebrado | CONCEPT-PERMISSIONS §3 ("Aprovador Diretor — cadeia de alçadas — ver [[CONCEPT-ALCADAS]]") | Criar CONCEPT-ALCADAS (mesmo que stub) OU remover o link. Alçadas aparecem implicitamente no Aprovador Diretor mas não há modelo. |
| 2 | `[[CONCEPT-AGENTS]]` é o nome usado em alguns lugares e `CONCEPT-ACTIONIA` é o nome do arquivo — termo dividido | Inconsistência | CONCEPT-ACTIONIA §0 nota a "tensão de escopo" mas tela77 / `AgentsList.jsx` usam "Agentes" | Padronizar nomenclatura: produto fala "Agentes (ActionIA)" ou só "ActionIA". Decidir e propagar em telas + concept. |
| 3 | `[[CONCEPT-ALERTS]]` (plural) vs `CONCEPT-ALERT` (singular) — links podem quebrar | Cross-reference quebrado | Verificar em todos os concepts: CONCEPT-STAGE-ROLE-ASSIGNMENT §0 referencia `[[CONCEPT-ALERT]]` (ok), mas outras telas usam "Alertas" | Manter `CONCEPT-ALERT` singular como canônico; conferir que todos os wikilinks usem essa forma. |
| 4 | Modo "ia_validacao" não bate com fluxo das 3 alternativas — fluxo descreve "IA + Validação" mas modelo registra `modo = ia_validacao` | Inconsistência leve | CONCEPT-MATCH-ENGINE §3.1 vs §2.1 | Renomear modo do schema para `ia_com_validacao` ou padronizar nome no fluxo. |
| 5 | `entity_type` em `user_scope` listado como enum, mas USER-SCOPE atualizado afirma "não é mais hardcoded — vem do catálogo de critérios" | Inconsistência (auto-introduzida nesta sessão) | CONCEPT-USER-SCOPE-INTERVENIENTES §2 | Atualizar tabela: `entity_type` passa de "enum" para "string (FK lógica para `criterio_catalogo.codigo`)". |
| 6 | "Gestor da cadeira" referencia `roles.parent_role_id` mas Permissions §2 não tem esse campo no modelo | Gap de definição | CONCEPT-STAGE-OWNERSHIP-VALIDATION §5.2 vs CONCEPT-PERMISSIONS §2 | Adicionar `parent_role_id` na tabela `roles` em CONCEPT-PERMISSIONS para suportar hierarquia. |
| 7 | "Função de escalação" (CONCEPT-STAGE-ROLE-ASSIGNMENT §3.3) cita [[CONCEPT-ALERT]] tipo "stage sem responsável", mas nenhum alerta default está catalogado | Gap de definição | STAGE-ROLE-ASSIGNMENT §3.3 + CONCEPT-ALERT | Adicionar catálogo de alertas default no CONCEPT-ALERT (similar ao TODO-WORKFLOWS-DEFAULT). |
| 8 | "Gestão de Profile" mencionada em 4 concepts (PERMISSIONS, USER-SCOPE, WORKFLOW-OBLIGATORY indireto, STAGE-OWNERSHIP-VALIDATION) sem doc próprio | Conceito mencionado sem doc | PERMISSIONS §3, §5; USER-SCOPE §4; STAGE-OWNERSHIP-VALIDATION §5 | Criar CONCEPT-GESTAO-PROFILE OU dedicar seção no PERMISSIONS explicando o role + responsabilidades (recebe órfãos, edita profile de outros). |
| 9 | "Cards órfãos" (NF sem CC etc.) mencionado em vários concepts sem fluxo formal de tratamento | Gap de definição | USER-SCOPE §5, WORKFLOW-OBLIGATORY §3.3, STAGE-ROLE-ASSIGNMENT §3.3 | Criar CONCEPT-ORPHAN-HANDLING OU unificar dentro do CONCEPT-WORKFLOW-OBLIGATORY (já tem `is_orphan`, falta o fluxo de resolução). |
| 10 | ActionIA pode atuar como "função virtual" em stages — mas modelo de `funcao_responsavel_id` (FK `roles`) não tem ponte pra `agents` | Gap de definição | STAGE-ROLE-ASSIGNMENT §3 / §4.5 vs ACTIONIA §2 | Ou criar role virtual sintética por ActionIA em `roles`, OU adicionar coluna `actionia_id` (nullable, exclusiva com `funcao_responsavel_id`) em `workflow_stages`. Definir antes da implementação. |
| 11 | "Esforço estimado" varia de "Médio" a "Muito alto" mas não há tabela consolidada de roadmap | Gap de definição | Todos os concepts §5 | Criar `docs/ROADMAP-CONCEPTS.md` agregando estimativas + dependências + ordem sugerida. |
| 12 | `page_key` usado em SAVED-FILTERS e USER-PROFILE-PREFERENCES tem mesmo formato mas convenção não documentada | Gap de definição | SAVED-FILTERS §2, USER-PROFILE-PREFERENCES §2 | Documentar convenção (ex: `<recurso>.<acao>` ou `<tela>.<recurso>`) num lugar único — provavelmente apêndice no USER-PROFILE-PREFERENCES. |
| 13 | `team_id` em SAVED-FILTERS referencia entidade `teams` que nunca aparece em outros concepts | Conceito mencionado sem doc | SAVED-FILTERS §2 | Criar CONCEPT-TEAMS ou explicar relação Team vs Role no PERMISSIONS. Times são scope organizacional ou são apenas agrupamento de filtros? |
| 14 | Tela04 ("biblioteca de workflows") referenciada em STAGE-OWNERSHIP-VALIDATION e WORKFLOW-OBLIGATORY mas tela10 também é "workflow editor" — confusão entre listagem vs editor | Inconsistência | STAGE-OWNERSHIP-VALIDATION §5.1, WORKFLOW-OBLIGATORY §7.1 vs ACTIONIA §6 (tela10 editor) | Alinhar: tela04 = biblioteca/listagem; tela10 = editor. Verificar que nenhum concept confunde. |
| 15 | Permission `delegavel` é booleana, mas regras 7-9 introduzidas nesta sessão falam em "cadeia de delegação" sem modelo persistente | Gap de definição (auto-introduzido) | CONCEPT-PERMISSIONS §5 regras 7-9 | Adicionar tabela `permission_delegations` (granted_to, granted_by, permission_id, granted_at) para auditoria + revogação em cascata. |
| 16 | CONCEPT-ALERT §2 não tem campo `severidade_dispara_workflow_default` — não há gancho explícito entre alert e workflow default | Gap de definição | CONCEPT-ALERT §2 / STAGE-OWNERSHIP-VALIDATION §7 | Decidir: workflow default dispara alert OU alert dispara workflow default? Hoje ambos coexistem sem fluxo claro. |
| 17 | Match Engine §4 ("Unicidade obrigatória — ambiguidade é sempre erro") e Stage-Ownership §4.2 (conflito 2+ donos) compartilham filosofia mas não se referenciam | Gap de cross-reference | MATCH-ENGINE §4 ↔ STAGE-OWNERSHIP-VALIDATION §4.2 | Adicionar nota cruzada: ambos seguem princípio "ambiguidade nunca é resolvida arbitrariamente — vira alerta humano". |
| 18 | AI-GENERATION §4 cita "Privacidade — política de retenção do tenant" mas não há CONCEPT-TENANT-RETENTION nem CONCEPT-MULTITENANT | Conceito mencionado sem doc | AI-GENERATION §4, SAVED-FILTERS §7 (multi-tenant) | Criar CONCEPT-MULTITENANT cobrindo isolamento, retenção, escopo de Admin Geral por tenant. |
| 19 | Workflow on-the-fly (`created_inline = true`) mencionado em WORKFLOW-OBLIGATORY §3.2 e §4 — pendência "quem aprova?" segue aberta | Gap de definição | WORKFLOW-OBLIGATORY §3.2 / §7 pendências | Resolver pendência antes da implementação: auto-aprovado por owner OU exige revisão por admin? |
| 20 | Mockups do tela50 / tela80 / tela81 / tela82 aparecem como "a criar" em vários concepts — sem checklist consolidado de telas faltantes | Funcionalidade implícita | Vários concepts §6-7 | Criar `docs/TELAS-PENDENTES.md` cruzando todas as telas mencionadas como "a criar" com o concept de origem. |
| 21 | "Multi-CC rateado" mencionado em STAGE-ROLE-ASSIGNMENT §5 ("NF com 2 CCs rateados") mas não há concept que modele rateio | Funcionalidade implícita sem concept | STAGE-ROLE-ASSIGNMENT §5 | Verificar se rateio existe no front (NF, requisição). Se sim, criar CONCEPT-RATEIO. Se não, remover edge case. |
| 22 | "i18n / PT-BR" mencionado em `correcoes-mockup.md` mas nenhum concept fala de internacionalização | Funcionalidade implícita sem concept | Mockups (i18n toggle) vs ausência | Criar CONCEPT-I18N curto: chaves, fallback, persistência por usuário, telas afetadas. |
| 23 | Stage `can_be_automated = true` em STAGE-ROLE-ASSIGNMENT mas ActionIA não tem flag "elegível para automação de stages" | Gap de definição | STAGE-ROLE-ASSIGNMENT §2 / §4.5 vs ACTIONIA §2 | Adicionar em `agents`: flag `can_replace_stage_user` (bool) para evitar que qualquer ActionIA seja silenciosamente atribuída a stage. |
| 24 | Histórico/audit mencionado em quase todos os concepts (USER-SCOPE §4.4, ACTIONIA §4, ALERT, etc.) mas sem concept único de auditoria | Gap de definição | Vários | Criar CONCEPT-AUDIT-LOG (tabela canônica, retenção, query patterns) para evitar implementações divergentes. |

---

## Sumário — Top 5 prioritários

1. **#15 + #6 — Modelos faltantes em PERMISSIONS** (adicionar `parent_role_id` em `roles` + tabela `permission_delegations`). Sem isso, delegação e "gestor da cadeira" ficam sem suporte de dados.
2. **#10 + #23 — Ponte ActionIA ↔ Stage** (resolver como ActionIA ocupa `funcao_responsavel_id`). Bloqueia implementação de stages automatizados.
3. **#1 + #19 — Alçadas e workflow on-the-fly** (criar CONCEPT-ALCADAS, decidir aprovação de workflow inline). Bugs prováveis em produção sem definição.
4. **#8 + #9 — Gestão de Profile / Órfãos** (formalizar role "Gestão de Profile" e fluxo de resolução de órfãos). 4+ concepts dependem disso implicitamente.
5. **#20 — TELAS-PENDENTES.md** (consolidar todas as telas "a criar" mencionadas nos concepts). Sem isso, mockup avança sem visão única de cobertura.

---

## Notas metodológicas

- Achados sinalizados como "auto-introduzido nesta sessão" (#5, #15) decorrem das atualizações feitas em 2026-05-31 e devem ser corrigidos na próxima rodada — não regrediu nada que estava certo antes, mas as edições abriram pontas soltas.
- Concepts mais "limpos" hoje: CONCEPT-SAVED-FILTERS, CONCEPT-USER-PROFILE-PREFERENCES (escopo bem delimitado, poucas dependências).
- Concepts mais "ricos em dependências": CONCEPT-STAGE-OWNERSHIP-VALIDATION (5 dependências), CONCEPT-STAGE-ROLE-ASSIGNMENT (6 dependências). Risco: refactor em qualquer dependência exige revisita destes dois.
